import { render, screen } from "@testing-library/react"
import Card from './Card'
test("Teste Footer", () => {
  // render(<Card dentista={{matricula: 123, nome: 'ettore', sobrenome:'muniz'}} />)

  // const text = screen.getByText('ettore')
  
  // expect(text).toBeInTheDocument()

})